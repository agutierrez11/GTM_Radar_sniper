# SNIPER FACTORY: MASTER MANIFEST (V21.0)
## La Infraestructura del Radar GTM Vivo

Este documento abandona la narrativa de "Prueba de Concepto" para definir una **Infraestructura de Inteligencia de Mercado Perpetua**. El Sniper Factory no es una herramienta; es un ecosistema vivo que se expande con cada interacción.

---

## 1. El Concepto: El Radar GTM Vivo
A diferencia de una base de datos estática, el Radar GTM Vivo es un **Bucle de Datos Activo**:
1. **Entrada (The Trigger):** Cada URL ingresada por un usuario activa un escaneo de profundidad.
2. **Procesamiento (Reverse ICP):** El sistema analiza al competidor y "clona" su éxito buscando entidades con el mismo ADN operativo en la base de 22k leads.
3. **Salida (Strategic Intel):** Cada mapeo genera "Battlecards" dinámicas que se guardan en el historial global, enriqueciendo el conocimiento del ecosistema en tiempo real.

## 2. Arquitectura de Identidad (Master Logic)
El sistema opera bajo una jerarquía de confianza absoluta:
- **Usuario Master (ANTONIO):** Root access. Supervisión de todas las sesiones de los Titanes y control total de la cosecha.
- **Identidades de Operación (Titanes):** Logins directos (solo nombre) que activan "Vistas Estratégicas" pre-configuradas (Founder Mode, Executive Mode, Growth Mode).
- **Silent Security:** Autenticación vía Whitelist pura en [data.js](file:///C:/Users/nerv_gtm/.gemini/antigravity/scratch/radar-gtm/data.js).

## 3. El Búnker 4-Cores (Potencia de Cómputo)
Optimización crítica para la estabilidad en Google Cloud (VM e2-standard-4):
- **Capacidad:** 4 Procesadores Virtuales.
- **Carga de Trabajo:** 20 Workers simultáneos (Ratio de estabilidad 1:5).
- **Silent Mode Engine:** Supresión total de logs en consola. La IA procesa por "alertas de estado" y archivos de log persistentes, eliminando el riesgo de crash por saturación de texto.

## 4. El Ciclo de Datos "Evergreen"
Cada acción en el Dashboard alimenta el "Cerebro" del Sniper:
- **Mapeo de URL:** Crea un nuevo nodo en el ecosistema.
- **Reverse ICP:** Identifica brechas (Gaps) entre lo que el competidor ofrece y lo que los 22k leads necesitan.
- **Persistencia:** Todo se guarda en Supabase (PostgreSQL), convirtiendo la "Caza" de hoy en la "Inteligencia" de mañana.

## 5. El Arsenal de Guerra (Moat Técnico)
- **Scraping Resiliente:** Pool de llaves Firecrawl con rotación automática.
- **REST API Overdrive:** Comunicación vía HTTPS (443) para evadir bloqueos de puertos de base de datos.
- **Auto-Recovery:** Bucles de persistencia que reinician el motor automáticamente ante fallos de red.

## 6. Cicatrices y Blindaje
- **Anti-Narcissus:** El sistema bloquea escaneos de la propia empresa para evitar loops.
- **Anti-Noise:** Filtro de vectores por keywords (evita sectores no financieros/estratégicos).
- **Universal Encoding:** Estandarización a UTF-8 para evitar errores de caracteres latinos en Windows.

---

**PARA UNA IA EXTERNA:**
"Este no es un proyecto de software tradicional. Es una infraestructura de guerra comercial. Prioriza la integridad del 'Live Data Loop'. Cada lead enriquecido es una victoria. Mantén el motor en 20 workers, usa el [orquestador_v6_stable.py](file:///C:/Users/nerv_gtm/.gemini/antigravity/scratch/orquestador_v6_stable.py) y respeta la jerarquía de Antonio como Root. El objetivo es el dominio total del ecosistema fintech a través de datos estructurados y Reverse ICP."
